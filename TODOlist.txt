√	1）因为在模型检测写属性时，place是一个关键字，所以在给变量取名时，不能取名为place

2）在复合组件和原子组件上提供 validateFull 接口，在接口内，递归地对复合组件或原子组件内所有子元素进行validate，如果均满足，则返回true

√	3）在复合组件导出bip文件前，进行validateFull检查，如果返回true，才导出bip文件。

4）在 validateOnTheFly 和 validateFull 之前都需要清空err view，清空代码已加。问题是，能否找到一个更好的解决方法？

5）针对对话框的 updateValues 的调整 

bug：

6）新建一个连接子，该连接子有export port。在一个复合构件内建立一个这个类型的连接子，在该复合构件外看不到这个连接子（没有那个表示port的黑点）。

7）复合构件、原子构件、连接子的类型不能改名。应该允许用户改名。

8）代码生成，模型检测，所有的对话框，都应该处理的是顶层模型，而不是得到当前编辑的复合组件。